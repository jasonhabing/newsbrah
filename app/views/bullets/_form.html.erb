<%= form_for(@bullet) do |f| %>
  <% if @bullet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bullet.errors.count, "error") %> prohibited this bullet from being saved:</h2>

      <ul>
      <% @bullet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :big_story_id %><br />
    <%= f.number_field :big_story_id %>
  </div>
  <div class="field">
    <%= f.label :rank %><br />
    <%= f.number_field :rank %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
