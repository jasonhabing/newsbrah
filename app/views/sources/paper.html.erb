<!-- header -->
<div class="container" style="padding-top: 20px">
<%= render 'header' %>
</div>

<!-- first story -->
<% big = @bigs[0] %>
<% feed = FeedItem.where(:id => big.bestfeed).first %>
<% if feed.nil? %>
<% feed = big.feed_items.first %>
<% end %>
<div class="container" style="padding-top: 20px">
	<div class="col-md-6">
	<a href="/story/<%= big.id %>"><%= image_tag(feed.imageurl, :width => "100%", :class => "lead_image") %></a><br><br>
	</div>

	<div class="col-md-6">
	<div class="lead-title"><b><a href="/story/<%= big.id %>"><%= feed.title %></a></b></div>
	<div class="lead-mentions">Mentioned in <%= big.feed_items.count %> news outlets
	</div>
	<div class="lead-description"><%= feed.desc %></div>
</div>


<!-- Stories 2 - 10 -->
<div class="clearfix"></div>
<% i = 1 %>
<% big = @bigs[1..8].each do |big| %>
<% feed = FeedItem.where(:id => big.bestfeed).first %>
<% if feed.nil? %>
<% feed = big.feed_items.first %>
<% end %>

<div class = "col-md-6" style="padding-top:30px">
<div class="visible-xs">
<a href="/story/<%= big.id %>"><%= image_tag(feed.imageurl, :width => "100%") %></a><br><br>
</div>
	<div class="story-title">
	<b><a href="/story/<%= big.id %>"><%= feed.title %></a></b>
	</div>
	<div class="story-mentions">
	Mentioned in <%= big.feed_items.count %> news outlets
	</div>
	<div class="story-description">
	<%= feed.desc %>
	</div>
<div class="visible-xs">
<br><br>
</div>
</div>
<% if [2, 4, 6, 8].include? i %>
<div class="clearfix"></div>
<% end %>
<% i = i + 1 %>
<% end %>

<div class="container" style="padding-top: 20px">
<%= render 'footer' %>
</div>
