<%= form_for(@source) do |f| %>
  <% if @source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@source.errors.count, "error") %> prohibited this source from being saved:</h2>

      <ul>
      <% @source.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :"Name of the feed:" %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :"RSS feed URL: (include 'http://')" %><br />
    <%= f.text_field :rss %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
